apiVersion: apps/v1
kind: Deployment
metadata:
  name: sumitg2386/fastapi-app:latest
  labels:
    app: fastapi
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fastapi
  template:
    metadata:
      labels:
        app: fastapi
    spec:
      containers:
      - name: sumitg2386/fastapi-app:latest
        image: sumitg2386/fastapi-app:latest:latest  # Replace with your Docker image
        ports:
        - containerPort: 8000
